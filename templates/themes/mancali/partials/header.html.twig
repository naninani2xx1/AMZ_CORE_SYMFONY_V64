<nav class="navbar navbar-expand-xl sticky-top" id="mynavbar">

    <div class="container-xxl">

        <a class="logo-brand me-xl-5 me-0" href="#">

            <img src="{{ asset(get_setting_by_key('logo-website').settingValue) }}" class="logo-image img-fluid" alt="logo">

        </a>

        <div class="navbar-toggler-holder d-xl-none">

            <div class="top-menu d-flex d-xl-none">
                <a class="nav-link" href="#" target="_blank"><i class="icon icon-facebook-ic"></i></a>
                <a class="nav-link" href="#" target="_blank"><i class="icon icon-linked-ic"></i></a>
                <a class="nav-link" data-bs-toggle="modal" data-bs-target="#openSearch"><i class="icon icon-search-ic"></i></a>
            </div>


            <button class="navbar-toggler collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">

                <span class="navbar-toggler-icon"><i class="zmdi zmdi-menu"></i></span>

                <span class="btn-close"><i class="zmdi zmdi-close"></i></span>

            </button>
        </div>

        <!-- top menu -->
        <ul class="top-menu">
            <li class="nav-item">
                <a class="nav-link" href="{{ get_setting_by_key('facebook').settingValue }}" target="_blank"><i class="icon icon-facebook-ic"></i></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ get_setting_by_key('linked').settingValue }}" target="_blank"><i class="icon icon-linked-ic"></i></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" data-bs-toggle="modal" data-bs-target="#openSearch"><i class="icon icon-search-ic"></i></a>

            </li>
        </ul>


        <!-- main menu -->
        {% import 'themes/mancali/partials/macros.html.twig' as macros %}
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav order-0 order-md-1">
                {% set menus = get_menus_by_position(menu_position_main) %}
                {% for menu in menus %}
                    {% if menu.children is not empty %}
                        {{ macros.get_multiple_menu(menu) }}
                    {% else %}
                        {{ macros.get_single_menu(menu) }}
                    {% endif %}
                {% endfor %}
            </ul>
        </div>
    </div>
</nav>